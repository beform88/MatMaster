[project]
name = "matmaster"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.7.0",
    "opik>=1.8.6",
    "bohr-agent-sdk>=0.1.23",
]

[project.packages]
packages = ["agents"]
